# 18 × 30 mm 缩板尝试记录

2026-09-08，目标是在电源阻容统一为 0402 后，将当前 20.5 × 32.5 mm PCB 尝试缩至 18 × 30 mm。

**历史结果：已完成目标尺寸的器件试排和多轮布线尝试，但没有完成全板连通。当时主工程保留 20.5 × 32.5 mm，仅合入通过检查的 0402 修改。随后用户决定将 C8、C19、C20、C22、C23 恢复为 0603，现已执行。后来主板另从板头收短 0.5 mm 至 20.5 × 32.0 mm；以下 18 × 30 mm 候选与报告保留原样作为历史证据。** 这次尝试不能证明 18 × 30 mm 在其他布局下不可实现。

## 候选布局与约束

- 目标面积 540 mm²，较当前 666.25 mm² 减少约 18.95%。
- 陶瓷天线横向居中，板头全层净空 18 × 4 mm；晶振与负载电容保留在主控右侧。
- 两排 2.00 mm 排针从背面安装，候选列中心距 15.70 mm；编号和网络顺序保留。
- Flash 与 USB TVS 保留在背面，TVS 旋转以缩短占用高度；两颗按键保留在正面。
- 电源、主控去耦和局部器件重新安排，射频、时钟、USB 与主控 Flash 关键连接单独布线。
- 保留四层及 L2 接地参考，信号 0.15 mm、非 GND 电源 0.20 mm；没有降低正式工程的铜间距、孔距及阻焊开窗要求。

## 检查结果和未完成项

目标板框、器件庭院、射频/晶振层使用、晶振间距与接地参考、排针编号、0402 封装和孔边到 SMD 开窗等几何检查通过。经过三轮全局布线及一轮局部补线，最后候选仍有 **18 处未连接、17 项 DRC 违规**；独立 QA 总体未通过。

未连接包括部分 Flash 串联电阻到背面 Flash 的连接、主控 GPIO 及部分供电支路。Flash 的背面中心焊盘限制了正面串联电阻附近的换层位置，主控两侧现有扇出和器件进一步压缩了绕线空间。局部补线在这些固定位置未找到合规通道。

17 项 DRC 为 9 项板边退铜不足、7 项单端未接通的过孔及 1 项短悬空线段。板边问题的实际间距约 0.2083 mm，小于现有 0.25 mm 规则。另外，局部补线新增普通孔尚未全部显式设为双面盖油。没有豁免上述问题，也没有把候选替换为主工程。

若以后继续尝试该尺寸，需要重新分配 Flash 串联电阻和主控的出线空间，并调整排针外侧走线余量；仅缩小板框或继续微调现有走线不足以形成可交付结果。本轮没有实板、射频或电源性能测量。

## 本地证据

临时候选、备份和原始日志均在已忽略的本地目录，未作为生产输出：

- [未完成的 18 × 30 mm PCB](tmp/power-0402-18x30-20260908-101555/work/esp32-c6-pico.kicad_pcb)
- [候选 DRC](tmp/power-0402-18x30-20260908-101555/work/output/drc-final.json) 与 [候选 QA](tmp/power-0402-18x30-20260908-101555/work/output/qa.json)
- [候选正面试排图](tmp/power-0402-18x30-20260908-101555/compact-front.svg) 与 [背面试排图](tmp/power-0402-18x30-20260908-101555/compact-back.svg)
- [局部补线结果](tmp/power-0402-18x30-20260908-101555/finish-compact-01.json.results.json)
- 同目录的 `*.command.json`、`*.stdout.log`、`*.stderr.log` 和 `*.exit-code.txt` 保留每轮命令及未经改写的输出。

当前主工程已将上述五颗大容量电容恢复为 0603，其余 18 个相关阻容保留原有 0402；恢复记录见 [POWER_PASSIVES.md](POWER_PASSIVES.md)。
