# 阻容封装约定与变更记录

2026-09-10 最新 RGB 原理图：按用户确认采用官方 DevKitC-1 v1.4 阻值，R9 从 10 kΩ 改为 **3.3 kΩ**（GPIO8 → +3V3），R15 从 330 Ω 改为 **0 Ω**（数据节点 → DIN），R19 保持 **10 kΩ**（数据节点 → VBUS_USB）。三者均保持 0402 与默认装配；R/C 数量仍为 40。原理图、设计清单、BOM 和本页链接的阻容清单已同步，PCB 与贴装坐标留待用户 layout 后更新。见 [GPIO8 原理图记录](output/rgb-gpio8-schematic.json)。

2026-09-08，按用户最新决定：**射频匹配部分保持 0201，其余全部电阻、电容统一英制 0402（公制 1005，标称本体 1.0 × 0.5 mm）**。板框保持 20.5 × 32.0 mm。

## 当前范围

| 范围 | 位号 | 当前封装 |
| --- | --- | --- |
| 射频匹配 R/C，不改动 | C2、C3、C16、C17、C18、R6 | 0201 |
| 大容量电源电容，本次由 0603 改为 0402 | C8、C19、C20、C22、C23 | 0402 |
| Flash 串联电阻，本次由 0201 改为 0402 | R17、R1–R5 | 0402 |
| 其余非射频 R/C | C4–C7、C9–C15、C21、C24、C25、R7–R11、R14、R15、R19 | 0402 |
| 晶振串联电阻，2026-09-09 由原 L4 更正 | R18 = 0 Ω，对应官方模组 R4 | 0402 |

当前全部 R/C 共 40 个：6 个保留 0201，34 个使用 0402。2026-09-10 已按用户原理图删除 USB 的 R12、R13、C27、C28；同日 RGB 驱动改为二极管电平转换，删除 R16、C26 并新增 R19 = 10 kΩ 上拉，见 [USB 同步记录](output/usb-layout-sync.json) 与 [RGB 二极管驱动记录](output/rgb-diode-drive.json)。2026-09-08 有 11 个器件更换封装；2026-09-09 另将晶振串联器件从 L4 = 24 nH 电感改为 R18 = 0 Ω 电阻，采用标准 `Resistor_SMD:R_0402_1005Metric`，连接不变。依据为模组手册第 37、38 页的 R4，详见 [外围电路复核](docs/ESP32_C6_SCHEMATIC_REVIEW.md)。射频 L1、L3 及其他电感、芯片、保护器件保持原样。

原理图、PCB、设计清单与 BOM 同步，保持数值、网络和 DNP 状态。Flash 串阻在背面以 1.20 mm 间距对齐成一排，局部重新扇出；U3 与 D4 各向 USB 方向移动 0.80 mm，C7 位于正面的 VDD_SPI 扇出旁。U3 的两个填孔盖铜接地孔随有效焊盘位置调整，仍只保留全板 11 个该工艺例外。所有信号走线仍为 0.15 mm，非 GND 电源仍为 0.20 mm；普通过孔孔边到正反面 SMD 开窗至少 0.10 mm。

[output/passive-packages.csv](output/passive-packages.csv) 记录当前全部 40 个 R/C 的封装；[output/power-passives.csv](output/power-passives.csv) 保留 23 个电源、EN 定时及 USB-C 供电识别阻容的子集（C26 换为 R19）。`BeforeFootprint` 对原有器件指 2026-09-08 调整前，对新增记录 R18 指 2026-09-09 更正前的 L4 电感封装；`Footprint` 为当前封装，实际变化标记 `Changed=YES`，累计 12 条。独立 QA 按上述全部 R/C 规则核对，并检查 R18 的电阻符号、0 Ω 数值、装配状态和晶振网络。

## 大容量电容

| 位号 | 用途 | 标称容量 | 最低额定耐压 |
| --- | --- | --- | --- |
| C8 | 3.3V 主电源储能 | 10 µF | 10V |
| C19、C20 | V3A 射频电源储能 | 各 10 µF | 10V |
| C22 | 稳压器输入储能 | 10 µF | 10V |
| C23 | 稳压器输出储能 | 22 µF | 10V |

上述封装为 `Capacitor_SMD:C_0402_1005Metric`。BOM 的通用 MPN 仍为占位，未完成采购选型。此前查到的 Murata GRM155R61A106ME11（10 µF / 10V）与 GRM158R61A226ME15（22 µF / 10V）均为 X5R 参考系列，分别记载 0.7 / 0.8 mm 最大高度；仅说明此封装与容量组合存在，不代表选定具体可采购料号。

历史来源：[Murata 官方器件清单](https://www.murata.com/-/media/webrenewal/tool/library/common-pdf/dynamic-model/component-list-d-mlcc-2506.ashx?cvid=20250805040419000000&la=en)。采购时须按最新资料核对用途限制、温度范围、供货、完整后缀与直流偏压曲线，不能把大容量参考件写成已选定的 X7R。

0402 MLCC 在实际 3.3V / 5V 偏压下的有效容量可能明显低于标称值。样板应核对稳压器稳定性、无线发射时电源压降和纹波；目前没有实板有效容量、电源或射频性能测量结论。标准库 3D 模型只表示位置，不能代替实际器件高度确认。

## 历史经过

1. 2026-09-08，为尝试 18 × 30 mm，将五颗大容量电容由 0603 改为 0402；缩板候选未完成布线，记录见 [SHRINK_TRIAL.md](SHRINK_TRIAL.md)。
2. 同日曾恢复五颗 0603 以便焊接；随后板头收短，尺寸变为 20.5 × 32.0 mm。
3. 本次按最新决定统一非射频 R/C 为 0402，并把 U1 相关外围移至主原理图用实线连接。此前恢复 0603 的决定不再是当前约定。

当前检查结果以 [QA](output/qa.json)、[DRC](output/drc-final.json) 和 [ERC](output/erc.json) 为准。修改前备份与逐次命令、原始输出位于本地 `tmp/nonrf-0402-direct-schematic-20260908-151123/`。
